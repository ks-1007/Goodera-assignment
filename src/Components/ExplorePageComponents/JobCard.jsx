import React from "react"
import styles from "./JoblistSection.module.css"
function JobCard({ title, image, description }) {
  return <div className={styles.jobCardRoot}></div>
}

export default JobCard
